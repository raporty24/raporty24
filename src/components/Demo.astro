<section id="demo-section" class="py-24 bg-white overflow-hidden">
  <div class="container mx-auto px-4">
    
    <header class="text-center mb-20">
      <span class="eyebrow">Dane w akcji</span>
      <h2 class="display-title">Przetestuj nasze dashboardy</h2>
      <p class="text-xl text-slate-500 mt-6 max-w-2xl mx-auto">
        Kliknij w miniaturę, aby otworzyć pełnoekranowe, interaktywne demo raportu Power BI.
      </p>
    </header>

    <div class="grid lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
      
      <div 
        class="group relative cursor-pointer"
        @click="openDemo(0)"
      >
        <div class="overflow-hidden rounded-3xl bg-slate-100 transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-green-600/10 group-hover:-translate-y-2">
          <div class="relative aspect-[16/9]">
            <img 
              src="/dash1.webp" 
              alt="Dashboard Magazyn & Sprzedaż" 
              class="object-cover w-full h-full transition-transform duration-700 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-slate-900/0 group-hover:bg-slate-900/40 transition-all duration-300 flex items-center justify-center">
               <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center opacity-0 scale-50 group-hover:opacity-100 group-hover:scale-100 transition-all duration-300 shadow-xl">
                  <i class="fas fa-play text-green-600 ml-1"></i>
               </div>
            </div>
          </div>
          <div class="p-8 bg-white border-t border-slate-50">
            <h3 class="text-2xl font-bold text-slate-900 mb-2">Dashboard Magazyn & Sprzedaż</h3>
            <p class="text-slate-500 text-sm">Analiza rotacji towaru, dostępności i trendów zakupowych.</p>
          </div>
        </div>
      </div>

      <div 
        class="group relative cursor-pointer"
        @click="openDemo(1)"
      >
        <div class="overflow-hidden rounded-3xl bg-slate-100 transition-all duration-500 group-hover:shadow-2xl group-hover:shadow-green-600/10 group-hover:-translate-y-2">
          <div class="relative aspect-[16/9]">
            <img 
              src="/dash2.webp" 
              alt="Dashboard Sprzedaż eCommerce" 
              class="object-cover w-full h-full transition-transform duration-700 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-slate-900/0 group-hover:bg-slate-900/40 transition-all duration-300 flex items-center justify-center">
               <div class="w-16 h-16 bg-white rounded-full flex items-center justify-center opacity-0 scale-50 group-hover:opacity-100 group-hover:scale-100 transition-all duration-300 shadow-xl">
                  <i class="fas fa-play text-green-600 ml-1"></i>
               </div>
            </div>
          </div>
          <div class="p-8 bg-white border-t border-slate-50">
            <h3 class="text-2xl font-bold text-slate-900 mb-2">Dashboard Sprzedaż eCommerce</h3>
            <p class="text-slate-500 text-sm">Monitoring marży, zwrotów i skuteczności kampanii w czasie rzeczywistym.</p>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<div
  x-cloak
  x-show="showDemo"
  class="fixed inset-0 z-[100] flex items-center justify-center p-4 md:p-10"
  @keydown.escape.window="closeDemo()"
>
  <div 
    class="absolute inset-0 bg-slate-900/90 backdrop-blur-sm"
    x-transition.opacity
    @click="closeDemo()"
  ></div>

  <div
    class="bg-white rounded-3xl overflow-hidden relative w-full h-full shadow-2xl z-10"
    x-transition.scale.95
  >
    <div class="absolute top-0 left-0 right-0 h-14 bg-white border-b border-slate-100 flex items-center justify-between px-6 z-20">
      <span class="font-bold text-slate-900 flex items-center gap-2">
        <i class="fas fa-chart-bar text-green-600"></i> Podgląd raportu
      </span>
      <button 
        @click="closeDemo()"
        class="w-8 h-8 flex items-center justify-center rounded-full bg-slate-100 text-slate-500 hover:bg-red-50 hover:text-red-600 transition-colors"
      >
        <i class="fas fa-times"></i>
      </button>
    </div>

    <div class="pt-14 w-full h-full">
      <iframe
        :src="demoSrc"
        class="w-full h-full border-0"
        title="Interaktywne Demo Power BI"
      ></iframe>
    </div>
  </div>
</div>